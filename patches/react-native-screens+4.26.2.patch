diff --git a/node_modules/react-native-screens/ios/gamma/split/RNSSplitAppearanceApplicator.swift b/node_modules/react-native-screens/ios/gamma/split/RNSSplitAppearanceApplicator.swift
index 36493d5..e85203f 100644
--- a/node_modules/react-native-screens/ios/gamma/split/RNSSplitAppearanceApplicator.swift
+++ b/node_modules/react-native-screens/ios/gamma/split/RNSSplitAppearanceApplicator.swift
@@ -28,7 +28,7 @@ class RNSSplitAppearanceApplicator {
     }
 
     appearanceCoordinator.updateIfNeeded(.secondaryScreenNavBarUpdate) { [weak self] in
-      guard let self = self
+      guard self != nil
       else {
         return
       }
diff --git a/node_modules/react-native-screens/ios/tabs/RNSTabBarAppearanceCoordinator.mm b/node_modules/react-native-screens/ios/tabs/RNSTabBarAppearanceCoordinator.mm
index fee81a8..7aaae52 100644
--- a/node_modules/react-native-screens/ios/tabs/RNSTabBarAppearanceCoordinator.mm
+++ b/node_modules/react-native-screens/ios/tabs/RNSTabBarAppearanceCoordinator.mm
@@ -149,6 +149,22 @@ - (void)updateTabBarItem:(nullable UITabBarItem *)tabBarItem
     return;
   }
 
+  // Bundled assets can bypass RCTImageLoader's requested-size decoding in Release.
+  // Bound custom tab images before handing them to UIKit; a layout pass alone
+  // does not shrink the resulting UIImage. Leave SF Symbols and asset catalogs alone.
+  CGSize requestedSize = (isSelected ? screenView.selectedIconImageSource : screenView.iconImageSource).size;
+  if (image != nil && requestedSize.width > 0 && requestedSize.height > 0 &&
+      image.size.width > 0 && image.size.height > 0 &&
+      (image.size.width > requestedSize.width || image.size.height > requestedSize.height)) {
+    CGFloat ratio = MIN(requestedSize.width / image.size.width, requestedSize.height / image.size.height);
+    CGSize fittedSize = CGSizeMake(image.size.width * ratio, image.size.height * ratio);
+    UIImageRenderingMode renderingMode = image.renderingMode;
+    UIGraphicsImageRenderer *renderer = [[UIGraphicsImageRenderer alloc] initWithSize:fittedSize];
+    image = [[renderer imageWithActions:^(UIGraphicsImageRendererContext *context) {
+      [image drawInRect:(CGRect){CGPointZero, fittedSize}];
+    }] imageWithRenderingMode:renderingMode];
+  }
+
   if (isSelected) {
     tabBarItem.selectedImage = image;
   } else {
