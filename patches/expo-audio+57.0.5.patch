diff --git a/node_modules/expo-audio/ios/AudioModule.swift b/node_modules/expo-audio/ios/AudioModule.swift
index d265606..38ef797 100644
--- a/node_modules/expo-audio/ios/AudioModule.swift
+++ b/node_modules/expo-audio/ios/AudioModule.swift
@@ -216,7 +216,7 @@ public class AudioModule: Module {
       }
 
       Function("setPlaybackRate") { (player, rate: Double, pitchCorrectionQuality: PitchCorrectionQuality?) in
-        let playerRate = rate < 0 ? 0.0 : Float(min(rate, 2.0))
+        let playerRate = rate < 0 ? 0.0 : Float(min(rate, 3.0))
         player.currentRate = playerRate
 
         if player.isPlaying {
@@ -591,7 +591,7 @@ public class AudioModule: Module {
     playerVolumes.removeAll()
 
     registry.allPlayables.forEach { playable in
-      if playable.isPlaying {
+      if playable.isPlaying || (playable as? AudioPlaylist)?.playbackRequested == true {
         interruptedPlayers.insert(playable.id)
         switch interruptionMode {
         case .duckOthers:
@@ -652,6 +652,10 @@ public class AudioModule: Module {
       }
       player.handleMediaServicesReset()
     }
+    registry.allPlaylists.values.forEach { playlist in
+      playlist.updateStatus(with: ["mediaServicesDidReset": true, "error": "Audio services reset"])
+      playlist.pause()
+    }
   }
 
   private func reconfigureAudioSession() {
@@ -694,7 +698,7 @@ public class AudioModule: Module {
 
   private func pauseAllPlayers() {
     registry.allPlayables.forEach { playable in
-      if playable.isPlaying {
+      if playable.isPlaying || (playable as? AudioPlaylist)?.playbackRequested == true {
         playable.wasPlaying = true
         playable.pause()
       }
diff --git a/node_modules/expo-audio/ios/AudioPlaylist.swift b/node_modules/expo-audio/ios/AudioPlaylist.swift
index 98dabcf..137cd77 100644
--- a/node_modules/expo-audio/ios/AudioPlaylist.swift
+++ b/node_modules/expo-audio/ios/AudioPlaylist.swift
@@ -11,6 +11,7 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
   let id = UUID().uuidString
   private let interval: Double
   private(set) var currentRate: Float = 1.0
+  private(set) var playbackRequested = false
 
   var loopMode: LoopMode = .none
   var wasPlaying = false
@@ -35,6 +36,7 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
   private var timeToken: Any?
   private var cancellables = Set<AnyCancellable>()
   private var endObserver: NSObjectProtocol?
+  private var failureObserver: NSObjectProtocol?
 
   weak var owningRegistry: AudioComponentRegistry?
 
@@ -76,12 +78,15 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
   }
 
   func play(at rate: Float) {
+    playbackRequested = true
     registerTimeObserver()
     ref.playImmediately(atRate: rate)
   }
 
   func pause() {
+    playbackRequested = false
     ref.pause()
+    updateStatus(with: ["playing": false, "playbackInterrupted": true])
   }
 
   func next() {
@@ -120,7 +125,7 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
   func seekTo(seconds: Double) async {
     let time = CMTime(seconds: seconds, preferredTimescale: CMTimeScale(NSEC_PER_SEC))
     await withCheckedContinuation { (continuation: CheckedContinuation<Void, Never>) in
-      ref.seek(to: time) { [weak self] _ in
+      ref.seek(to: time, toleranceBefore: .zero, toleranceAfter: .zero) { [weak self] _ in
         if let self {
           self.updateStatus(with: ["currentTime": self.currentTime])
         }
@@ -215,8 +220,9 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
   }
 
   func setPlaybackRate(_ rate: Float) {
-    let boundedRate = max(0.1, min(rate, 2.0))
+    let boundedRate = max(0.1, min(rate, 3.0))
     currentRate = boundedRate
+    ref.defaultRate = boundedRate
     if isPlaying {
       ref.rate = boundedRate
     }
@@ -236,7 +242,8 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
       "muted": ref.isMuted,
       "volume": ref.volume,
       "loop": loopMode.rawValue,
-      "didJustFinish": false
+      "didJustFinish": false,
+      "error": (ref.currentItem?.error?.localizedDescription as Any?) ?? NSNull()
     ]
   }
 
@@ -281,6 +288,8 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
         }
         if status == .readyToPlay {
           self.updateStatus(with: ["isLoaded": true])
+        } else if status == .failed {
+          self.updateStatus(with: ["error": "Playlist item unavailable", "playing": false])
         }
       }
       .store(in: &cancellables)
@@ -302,6 +311,13 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
   }
 
   private func addPlaybackEndNotification() {
+    failureObserver = NotificationCenter.default.addObserver(
+      forName: .AVPlayerItemFailedToPlayToEndTime, object: nil, queue: nil
+    ) { [weak self] notification in
+      guard let self, let item = notification.object as? AVPlayerItem,
+            self.playerItems.contains(where: { $0 === item }) else { return }
+      self.updateStatus(with: ["error": "Playlist item failed", "playing": false])
+    }
     endObserver = NotificationCenter.default.addObserver(
       forName: .AVPlayerItemDidPlayToEndTime,
       object: nil,
@@ -328,6 +344,7 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
 
       case .none:
         if isLastTrack {
+          self.playbackRequested = false
           self.updateStatus(with: [
             "playing": false,
             "didJustFinish": true
@@ -345,11 +362,11 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
     let updateInterval = interval / 1000
     let interval = CMTime(seconds: updateInterval, preferredTimescale: CMTimeScale(NSEC_PER_SEC))
 
-    timeToken = ref.addPeriodicTimeObserver(forInterval: interval, queue: nil) { [weak self] time in
+    timeToken = ref.addPeriodicTimeObserver(forInterval: interval, queue: nil) { [weak self] _ in
       guard let self else {
         return
       }
-      self.updateStatus(with: ["currentTime": time.seconds])
+      self.updateStatus(with: ["currentTime": self.currentTime])
     }
   }
 
@@ -374,6 +391,9 @@ public class AudioPlaylist: SharedRef<AVQueuePlayer>, Playable {
     if let endObserver {
       NotificationCenter.default.removeObserver(endObserver)
     }
+    if let failureObserver {
+      NotificationCenter.default.removeObserver(failureObserver)
+    }
 
     ref.pause()
     ref.removeAllItems()
